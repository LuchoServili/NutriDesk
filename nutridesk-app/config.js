// NutriDesk — Configuración de integraciones (frontend)
// Completá estos valores cuando tengas el proyecto de Supabase creado.
// La URL y la anon key son PÚBLICAS por diseño (la seguridad la da RLS).
// Si quedan en null, la app funciona en modo local (localStorage + login demo).
window.NUTRIDESK_CONFIG = {
  SUPABASE_URL: null,      // ej: 'https://abcdefgh.supabase.co'
  SUPABASE_ANON_KEY: null, // ej: 'eyJhbGciOi...'
};
